<----------branches----------->
remove branch remote?
git push origin :test
git push origin :dev

remove branch locally?
git branch -d test
git branch -d dev

to final remove locally:
git branch -D test
git branch -D dev

<---------------tags---------->


To list all tags:
git tag

To delete tag remote:
git push origin --delete v1.7


To delete tag locally:
git tag -d v1.7 
